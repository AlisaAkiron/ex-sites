# Public origin of the deployed site (no trailing slash).
# Inlined at build time by Vite (see `envPrefix` in vite.config.ts); used to
# build absolute og:url, og:image, canonical, sitemap, and robots URLs.
APP_URL=https://ex.alisaqaq.moe

# Rybbit analytics (public identifiers).
VITE_RYBBIT_HOST=https://rybbit.alisaqaq.moe/api
VITE_RYBBIT_SITE_ID=ba727e50a75b
